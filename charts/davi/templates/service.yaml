apiVersion: v1
kind: Service
metadata:
  name: {{ include "davi.fullname" . }}
  labels:
    {{- include "davi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "davi.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
